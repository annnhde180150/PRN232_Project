'use client';

import { PendingBookings } from '@/components/applications/PendingBookings';

export default function PendingBookingsPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <PendingBookings />
    </div>
  )
} 